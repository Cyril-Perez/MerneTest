const initalState = {
    _id : "",
    email : "",
    pseudo : ""
}

export default function fetchReducer(state= initalState, action ) {
    switch (action.type) {
        case "value":
            
            break;
    
        default:
            break;
    }
}